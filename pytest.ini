[pytest]
# Allow `pytest` to find both multiros (this package's src/) and uniros
# (the canonical home for the proxy class + shared utils).
# Adjust the uniros path if your workspace layout differs.
pythonpath =
    src
    ../../UniROS/uniros/src

testpaths = tests
addopts = -ra
